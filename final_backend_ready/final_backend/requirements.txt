# --- Web stack (Pydantic v2) ---
fastapi>=0.115,<1
starlette>=0.37,<1
uvicorn[standard]==0.35.0
typing-extensions>=4.7,<5
pydantic>=2.11,<3
pydantic-settings>=2.7,<3

# --- LLM / RAG ---
langchain==0.2.17
langchain-community
langchain-huggingface
langchain-groq
langsmith>=0.1.147
faiss-cpu

# --- Google clients (both v2-era) ---
google-genai==0.3.0
google-generativeai>=0.7.2

# --- OpenAI client (optional) ---
openai>=1.0,<2

# --- Data / plotting ---
pandas
numpy
openpyxl
xlrd==1.2.0          # NEW: legacy .xls reader (pandas engine)
XlsxWriter>=3.2.0    # NEW: optional, better Excel writing
matplotlib
seaborn
plotly
# kaleido               # Optional: enable Plotly static image export

# --- PDF / DOC tooling ---
pymupdf>=1.24,<2        # for `import fitz`
pdfplumber
tabula-py               # needs Java (JRE) installed
python-docx
pypdf

# --- File uploads / IO ---
python-multipart
aiofiles
Pillow>=10.3.0        # NEW: image metadata (width/height) for uploads
requests==2.31.0
python-dotenv==1.0.0

# --- Optional local models ---
gpt4all
transformers
torch             
       # CPU wheel will install; pin only if you know your CUDA
paddlepaddle==2.6.1
paddleocr==2.7.0.post1
pymupdf==1.24.9
opencv-python==4.10.0.84
python-dateutil==2.9.0.post0
requests==2.32.3
numpy==1.26.4

